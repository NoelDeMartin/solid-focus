{
    "extends": "@tsconfig/node22/tsconfig.json",
    "include": ["**/*.ts", "../src/**/*.ts"],
    "compilerOptions": {
        "baseUrl": ".",
        "module": "ESNext",
        "moduleResolution": "Bundler",
        "lib": ["DOM"],
        "types": ["node", "cypress"],
        "paths": {
            "@/*": ["../src/*"]
        }
    }
}
